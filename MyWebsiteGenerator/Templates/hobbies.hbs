<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mis Hobbies</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="../assets/css/styles.css">

    <style>
        .hobby-gallery {
            display: none; /* Las galerías están ocultas por defecto */
            gap: 10px;
            flex-wrap: wrap;
            justify-content: center;
            margin-top: 1rem;
        }

            .hobby-gallery img {
                width: 100px;
                height: 100px;
                object-fit: cover;
                border-radius: 1px;
                border: 1px solid #ddd;
                cursor: pointer;
                transition: transform 0.2s ease-in-out;
            }

            .hobby-gallery.visible {
                display: flex; /* La clase visible las muestra */
            }
    </style>
</head>
<body>
    {{> navigation}}

    <div class="container">
        <h1 class="text-center my-4">Mis Hobbies</h1>
        <div class="row">
            {{#each Hobbies}}
            <div class="col-md-6 col-lg-4 d-flex">
                <div class="card mb-4 w-100 p-4">
                    <h3 class="card-title">{{this.Nombre}}</h3>
                    <p class="card-text">{{this.Descripcion}}</p>
                    <hr>
                    <div class="text-center">
                        <button type="button" class="btn btn-primary toggle-gallery-btn">
                            Ver Galería
                        </button>
                    </div>

                    <div class="hobby-gallery">
                        {{#each this.Imagenes}}
                        <img src="{{this}}" alt="Imagen de {{../Nombre}}" class="img-thumbnail">
                        {{/each}}
                    </div>
                </div>
            </div>
            {{/each}}
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script src="../assets/js/main.js"></script>
</body>
</html>