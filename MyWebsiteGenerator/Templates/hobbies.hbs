<!DOCTYPE html>
<html>
<head>
    <title>Pasatiempos</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="/assets/css/styles.css">
</head>
<body>
    {{> navigation}}
    <div class="container mt-4">
        <h1>Pasatiempos</h1>
        <ul class="list-group">
            {{#each Hobbies}}
            <li class="list-group-item">
                <h4>{{Nombre}}</h4>
                <p>{{Descripcion}}</p>
                <div class="gallery">
                    {{#each Imagenes}}
                    <img src="{{this}}" alt="{{../Nombre}}" class="img-thumbnail">
                    {{/each}}
                </div>
            </li>
            {{/each}}
        </ul>
    </div>
</body>
</html>