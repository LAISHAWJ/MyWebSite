<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>YouTubers Favoritos</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">

    <link rel="stylesheet" href="../assets/css/styles.css">
</head>
<body>
    {{> navigation}}

    <div class="container">
        <h1 class="text-center my-4">YouTubers Favoritos</h1>
        <div class="row">
            {{#each YouTubers}}
            <div class="col-md-6 col-lg-4 d-flex">
                <div class="card mb-4 w-100 text-center p-4">
                    <!--Logo arriba -->
                    <img src="{{this.LogoUrl}}" alt="Logo de {{this.NombreCanal}}"
                         class="img-fluid mb-3 mx-auto channel-logo">

                    <!-- Nombre clickeable -->
                    <h3 class="card-title">
                        <a href="{{this.CanalUrl}}" target="_blank" class="channel-link">
                            {{this.NombreCanal}}
                        </a>
                    </h3>

                    <!-- Descripción oculta -->
                    <div class="description-container d-none mb-3">
                        <p class="card-text text-muted">{{this.Descripcion}}</p>
                    </div>

                    <!-- Botón para mostrar descripción -->
                    <div class="mt-auto">
                        <button class="btn btn-secondary toggle-description-btn">Ver Descripción</button>
                    </div>
                </div>
            </div>
            {{/each}}
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script src="../assets/js/main.js"></script>
</body>
</html>
