<!DOCTYPE html>
<html>
<head>
    <title>Series Favoritas</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="/assets/css/styles.css">
</head>
<body>
    {{> navigation}}
    <div class="container mt-4">
        <h1>Series Favoritas</h1>
        <ul class="list-group">
            {{#each Series}}
            <li class="list-group-item">
                <h4>{{Nombre}}</h4>
                <p>{{Descripcion}}</p>
                <img src="{{CaratulaUrl}}" alt="{{Nombre}}" class="img-thumbnail" width="150">
                <iframe src="{{TrailerUrl}}" width="300" height="200" frameborder="0" allowfullscreen></iframe>
            </li>
            {{/each}}
        </ul>
    </div>
</body>
</html>